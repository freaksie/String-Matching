File 1: Knuth_Morris.py
        It performes the string matching given input and pattern string.

File 2: Robin_Karp.py   
        It performes the string matching given input, pattern string and a prime number for mod.

File 3: main.py
        Declared input and pattern.
        Ran both the algorithms and saved their runtime performance.
        Creates graph for all the algorithms and also a comparison graph.

Steps to run:
	1. Install python environment and pip library matplotlib for graph ploting.
	2. Open terminal within the folder where files are saved.
	3. write command "main.py"